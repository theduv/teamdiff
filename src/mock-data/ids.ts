import { generateIDs } from "../lib/functions/generateIDs";

export const MOCK_SUMMONER_IDS = generateIDs(3);

export const MOCK_GRADES_IDS = generateIDs(3);

export const MOCK_REVIEWS_IDS = generateIDs(3);
